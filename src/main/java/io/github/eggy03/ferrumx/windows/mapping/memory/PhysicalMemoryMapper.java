package io.github.eggy03.ferrumx.windows.mapping.memory;

import io.github.eggy03.ferrumx.windows.entity.memory.PhysicalMemory;
import io.github.eggy03.ferrumx.windows.mapping.CommonMappingInterface;

/**
 * Provides a type-safe implementation of {@link CommonMappingInterface}
 * and maps JSON strings from Powershell to {@link PhysicalMemory} objects or lists of {@link PhysicalMemory} objects
 *
 * @since 2.3.0
 * @author Egg-03
 */
public class PhysicalMemoryMapper implements CommonMappingInterface<PhysicalMemory> {
}
